/**
 * Contains tests for top level {@link IInitialiser} classes and interfaces.
 * <br>
 * <br>
 * Concrete initialisers are not tested, since there are already mechanisms
 * implemented in them to ensure that they do what they are meant to do.
 */
package cipm.consistency.initialisers.tests;